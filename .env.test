# Test MongoDB Atlas Connection
# 
# INSTRUCTIONS:
# 1. Wait for your cluster to finish provisioning in MongoDB Atlas
# 2. Copy the connection string from the Atlas interface
# 3. Replace the MONGODB_URI below with your actual connection string
# 4. Make sure to replace <password> with your actual password
# 5. Add /finance_ai before the ? to specify the database name
#
# Example format:
# mongodb+srv://financeai-admin:yourpassword@cluster0.xxxxx.mongodb.net/finance_ai?retryWrites=true&w=majority

MONGODB_URI=mongodb+srv://financeai-admin:Dishu%4090415@cluster0.fnatvnd.mongodb.net/finance_ai?retryWrites=true&w=majority&appName=Cluster0

# Other settings for testing
NODE_ENV=development
PORT=5000
JWT_SECRET=test-secret-key-for-local-testing-only